describe('Feedback Loop', () => {
  it('Should confirm that true is equal to true', () => {
    expect(true).to.equal(true)
  });
});

describe('Loading Page View', () => {
  it('Should confirm that a loading page appears before data is fetched', () => {
    // cy.intercept('')
  });
});